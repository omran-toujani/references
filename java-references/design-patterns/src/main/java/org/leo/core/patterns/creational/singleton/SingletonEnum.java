package org.leo.core.patterns.creational.singleton;

/**
 * A variant of Singleton implementation based on enumeration
 * @author fahd
 */
public enum SingletonEnum {
  INSTANCE;// the implementation can really stop here !!! the rest is just for testing
  
  public String value;
}
